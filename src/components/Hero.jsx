// src/components/Hero.jsx
import React from 'react';

const Hero = () => {
  return (
    <div className="hero">
      <div className="container">
        <h2>Find Your Books Instantly</h2>
        <p>
          Scan the QR code outside the library to access our complete catalog. 
          Search, filter, and check availability of all books in our collection.
        </p>
      </div>
    </div>
  );
};

export default Hero;